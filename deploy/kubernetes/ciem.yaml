# Manifest legado — use os arquivos numerados nesta pasta.
#
# kubectl apply -f deploy/kubernetes/
#
# Arquivos atuais:
#   00-namespace.yaml … 09-ingress.yaml
#
# Documentação: docs/KUBERNETES.md e deploy/kubernetes/README.md
