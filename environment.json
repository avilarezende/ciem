{
  "install": "pip install -r requirements-dev.txt",
  "start": "export CONFIG_PATH=./config PYTHONPATH=shared:services/core && uvicorn app.main:app --host 0.0.0.0 --port 8000 --app-dir services/core"
}
